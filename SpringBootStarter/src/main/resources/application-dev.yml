server:
  port: 9999
  servlet:
    context-path: /devhp
    
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': false
    hibernate:
      ddl-auto: update        # Không nên dùng cấu hình này trong môi trường PRD.
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/first_db_devhp
    username: postgres
    password: P@ssw0rd

